
## 2026-04-28: 商品性能表数据注入与板块修复
- **物理修复内容**：成功将“大姐店”真实全店总量数据（曝光、点击、加车）注入到商品性能表板块。
- **技术细节**：
    - 后端：在 `api_server.py` 的 `/api/product_metrics` 接口中增加了针对 `group_label = '大姐店'` 的聚合查询。
    - 钩子：更新了 `useProductPerformance.js` 以支持对象化响应格式（`{ items, summary }`）。
    - 前端：修改了 `ProductPerformanceView.jsx`，将 KPI 卡片原本由前端计算的 `totalExp` 和 `totalCarts` 替换为后端返回的真实物理总量。
- **环境对齐**：通过 `/usr/local/bin/node` (v24.12.0) 绕过了 ARM64 下的 Rollup 签名错误，恢复了 Vite 的正常编译能力。
