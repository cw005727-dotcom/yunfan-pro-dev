# 云帆跨境 PRO：前端开发与协作准则 (FRONTEND_RULES.md)

## 🚨 核心开发准则 (Core Rules)
- **API 数据驱动**：所有动态内容（店铺名、订单状态、统计项、店铺列表）必须从接口获取，禁止在前端硬编码。
- **零映射显示**：后端返回什么文案，前端就直接显示什么。禁止在前端做英文到中文的硬映射（严禁 `status === 'paid'` 这种逻辑判断）。
- **动态视图渲染**：所有列表、卡片、监控胶囊必须使用 `.map()` 动态生成。禁止写死固定数量的 HTML 容器（如“固定 6 个声誉卡”）。

## 🛠️ 代码质量与性能规范 (Quality & Performance)
- **全局常量单一定义**：公共配置（如 `SITE_LIST`、站点旗帜映射）必须定义在脚本最顶层。严禁在子组件内部重复定义。
- **严禁“幽灵代码”**：在进行逻辑修改或回滚时，必须物理删除旧代码。禁止通过“注释掉”或“定义同名变量覆盖”的方式进行修改。
- **Lucide 渲染禁令**：禁止在组件 `useEffect` 中重复初始化 Lucide。必须使用封装好的 `Icon` 组件，并采用 `useRef` 定向渲染模式。
- **状态提升 (State Lifting)**：全局状态（如 `activeShop`、`shopList`）必须保留在根组件 `App`，严禁在子组件内私自维护可能冲突的全局状态副本。

## 🛡️ 容错与反馈规范 (Error Handling)
- **禁止静默失败**：所有的 `fetch` 请求必须链式调用 `.catch`。严禁使用空的 `catch(() => {})`。
- **强制 Toast 提示**：所有捕获到的错误必须通过 `showToast(msg, 'error')` 告知用户。
- **空状态保护**：所有数据渲染前必须进行 `null/undefined` 检查，确保在数据加载中或加载失败时，页面不崩塌、不白屏。

## 🤝 AI 协作与操作纪律
- **身份声明**：数据 AI 在传递后端指令或数据库规范时，开头必须声明“我是数据 AI，转达你...”。
- **先问后做**：任何影响系统或用户的重要操作（部署、重启、删除、修改核心配置）必须先说明方案，等用户确认后再执行。
- **版本归档**：重大 UI 调整前，必须将当前稳定的 `index.html` 备份至 `backups/` 目录。
