# 云帆Pro 可行性报告 v1.0

> 目标：把网站功能做成完整的、真实数据驱动的MercadoLibre多店管理后台。
> 原则：先确认数据能拿到，再动手做功能。**数据先行，功能后做。**

---

## 一、ML API 数据能力清单

### 1.1 能拿到的数据 ✅

| 数据 | 来源API | 刷新频率 | 入库表 |
|------|---------|---------|--------|
| 商品列表（名称/价格/图片/库存状态） | `/marketplace/items` | 手动/每天 | `product_metrics` |
| 商品访客量（曝光） | `/visits/items` | 手动/每天 | `product_metrics.exposure` |
| 订单列表 | webhook实时 + `/marketplace/orders/search` | 实时 | `orders_v2` |
| 物流状态 | `/marketplace/shipments/{id}?x-format-new=true` | 手动/每天 | `orders_v2` |
| 店铺声誉 | 定期镜像 JSON 注入 | 定期 | `stores` |

### 1.2 拿不到的数据 ❌

| 数据 | 原因 |
|------|------|
| clicks（点击量） | ML API 根本没有此端点 |
| carts（加车数） | ML API 根本没有此端点，orders返回的order_items永远为null |
| 商品维度转化率 | 无 clicks/carts 数据，无法计算 |
| 买家地址（精确） | shipments API 需要订单ID逐一查询，量大时不可行 |

**结论：产品表现表格里 clicks/carts 两列必须删掉，这是平台限制非代码问题。**

---

## 二、开发流程（强制顺序）

每个功能开发前，先回答三个问题：
1. 数据从哪里来？（确认API接口）
2. 写入哪个表哪个字段？
3. 前端怎么用？

三个问题都有答案了再动手。

### 阶段 1：数据层（数据AI单独完成）

**目标**：确认每个数据点能从哪里来，把数据写入数据库。

规则：
- 每个数据任务单独写脚本
- 先测试 API 返回格式
- 测试OK再跑批量
- 批量过程全程记录

产出：`/sync_xxx.py` 脚本 + 各表数据填充完整

### 阶段 2：接口层（数据AI完成）

**目标**：前端请求的数据都有对应的 `/api/xxx` 接口。

规则：
- `api_server.py` 里加 handler
- 先在 Postman/curl 里测试返回格式
- 测试OK再加到代码里

### 阶段 3：前端层（UI Agent 完成）

**目标**：把阶段2的数据以合适的方式展示出来。

规则：
- 功能AI给出接口文档（字段说明）
- UI Agent 按文档接数据
- 有真实数据再调样式

---

## 三、当前各功能块数据状态

### 3.1 产品表现（ProductPerformanceView）

**当前状态**：数据部分真实，访客正在拉取中。

| 字段 | 数据源 | 状态 |
|------|--------|------|
| 名称/价格/图片 | product_metrics | ✅ 真实 |
| 访客量（exposure） | `/visits/items` | 🔄 拉取中（82%） |
| 销量（sales） | orders_v2 推算 | ✅ 真实 |
| clicks | 无 | ❌ 删列 |
| carts | 无 | ❌ 删列 |

**待做**：访客拉完后删掉前端 clicks/carts 列。

### 3.2 物流追踪（LogisticsAlertsView / 各订单页）

**当前状态**：tracking_id/logistic_type/shipping_status 有，地址没有。

| 字段 | 数据源 | 状态 |
|------|--------|------|
| 物流状态 | orders_v2 | ✅ 已有 |
| 收件地址 | `/shipments/{id}` | ❌ 量大不可行 |
| 到货时间 | shipment.estimated_delivery | ✅ 已入库 |

**结论**：收件人地址字段暂时不接，数据量大时每单都要单独查API，性价比太低。

### 3.3 声誉中心（ShopReputationView）

**当前状态**：6站点数据通过定期镜像JSON注入，数据是旧的。

| 数据 | 状态 |
|------|------|
| 各站点声誉等级 | ✅ 已注入（需定期刷新） |
| 投诉/取消/延误率 | ✅ 已注入 |
| 新增数据 | ✅ 已注入 |

**结论**：声誉数据做成"手动刷新"按钮，每次点击触发 JSON 镜像注入。

### 3.4 关键词分析（KeywordIntelligenceView）

**当前状态**：MLB Trends 50条关键词真实，1688/Amazon/Temu 数据是假数据。

| 数据 | 来源 | 状态 |
|------|------|------|
| MLB 热搜词 | `/trends/MLB` | ✅ 真实 |
| 竞品平台数据 | 写死JSON | ⚠️ 假数据，标注数据来源 |

**结论**：竞品平台数据标注"第三方参考数据"，不花精力伪造。

---

## 四、今后开发规则

1. **任何功能开发前**，先在 #可行性确认 流程里走一遍：
   - 我（数据AI）确认数据源和写入格式
   - 产出 `sync_xxx.py`
   - 前端再接数据

2. **试错成本**：每个 API 测试不超过 30 分钟，测试脚本写完就跑，不要反复改脚本。

3. **不做的事**：
   - 不伪造任何平台数据
   - 不花精力做三方平台数据（Amazon/1688/Temu 标注"参考数据"即可）
   - clicks/carts 类数据永久不做

---

## 五、当前开发进度

| 功能 | 数据 | 接口 | 前端 | 状态 |
|------|------|------|------|------|
| 商品同步 | ✅ | ✅ | ✅ | 完成 |
| 商品访客量 | 🔄 82% | ✅ | 🔄 | 进行中 |
| 产品表现表 | ✅ | ✅ | ❌ 删carts/clicks | 进行中 |
| 物流追踪 | ✅ | ✅ | ✅ | 完成 |
| 声誉中心 | ✅ | ✅ | ✅ | 完成 |
| 关键词分析 | ✅ | ✅ | ✅ | MLB真实，其他标注 |
| 订单Webhook | ✅ | ✅ | ✅ | 完成 |

---

## 六、当前待办事项

### 高优先级
- [ ] 等待 sync_visits.py 跑完（还剩 ~18%，约5分钟）
- [ ] 删掉 ProductPerformanceView 前端的 clicks/carts 列
- [ ] git push

### 中优先级
- [ ] 定期镜像脚本（声誉数据自动刷新）
- [ ] shipments API 补充订单收件地址（如果数据量不大）

### 低优先级（平台限制，不做）
- [ ] 商品维度 clicks/carts - 永久放弃
